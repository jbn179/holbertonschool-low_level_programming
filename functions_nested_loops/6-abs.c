#include <stdlib.h>
#include <stdio.h>
#include "main.h"

/**
 * abs - computes the absolute value of an integer.
 * @n for number
 *
 * Description: computes the absolute value of an integer.
 */

int _abs(int);

{
	if (n > 0)
		return (n)
	else
		return (-n)
}
